apiVersion: v1
kind: Service
metadata:
  name: {{SERVICE_NAME}}-service
spec:
  selector:
    app: {{SERVICE_NAME}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP